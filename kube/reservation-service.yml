apiVersion: apps/v1
kind: Deployment
metadata:
  name: reservation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reservation-service
  template:
    metadata:
      labels:
        app: reservation-service
    spec:
      containers:
        - name: reservation-service
          image: tommimassa98/savefood-reservation-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: eureka.instance.preferIpAddress
              value: "true"
            - name: eureka.client.serviceUrl.defaultZone
              value: http://discovery-server:8761/eureka
            - name: axon.axonserver.servers
              value: axonserver-grpc:8124
---

apiVersion: v1
kind: Service
metadata:
  name: reservation-service
spec:
  selector:
    app: reservation-service
  ports:
    - port: 8084
      targetPort: 8084